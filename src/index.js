import { Text, View } from 'react-native';
import styled from '@emotion/native';

const X = styled.Text`
  color: red;
`;

export const VideoView = () => {
  return (
    <View>
      <X>123</X>
    </View>
  );
};
